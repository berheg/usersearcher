language: node_js
node_js:
  - 10.6.0
cache: npm
install:
  - npm install
script:
  npm test
  npm run build
deploy:
  provider: pages
  edge: true
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master
